<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!--
        Dịch vụ nổi: thứ duy nhất giữ cho tiến trình không bị MIUI đóng băng.
        POST_NOTIFICATIONS cho thông báo thường trực (Android 13+ mới đòi).
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- allowBackup=false: máy này giữ khóa cầu nối, đừng để lọt vào bản sao lưu đám mây. -->
    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            Trình nghe thông báo. Bắt buộc phải khai đúng permission và action này,
            nếu không mục "Quyền truy cập thông báo" của Android sẽ không thấy app.
        -->
        <service
            android:name=".BankNotificationListener"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!--
            targetSdk 34 nên BẮT BUỘC phải khai kiểu dịch vụ nổi.

            Chọn "specialUse" chứ KHÔNG "dataSync": Android 15 chặn dataSync 6
            tiếng/ngày, mà cầu nối phải sống suốt giờ mở cửa. App cài tay, không
            qua kiểm duyệt Play, nên specialUse dùng được thoải mái.
        -->
        <service
            android:name=".BridgeService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Theo doi thong bao ngan hang de tu chot don thanh toan" />
        </service>

        <service
            android:name=".RetryJobService"
            android:exported="false"
            android:permission="android.permission.BIND_JOB_SERVICE" />

        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
